# 🧠 Trionn 仿写项目 — 知识点全景图

> 回答一个问题：**"我到底学到了什么？"**
> 结论先说：你通过 963 行代码，接触了普通教程 **3~4 个阶段**的内容，
> 水平处于"能逆向商业作品"的工程师阶段，只是知识点是"用出来的"、还没系统化。

---

## 一、Three.js 渲染管线（基础但配置很高级）

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| 透视相机 | `PerspectiveCamera(35, 1, 0.1, 100)` | fov/宽高比/near/far，每帧 `updateProjectionMatrix()` |
| 颜色空间 | `renderer.outputColorSpace = SRGBColorSpace` | 正确色彩管理，不然颜色发灰 |
| 色调映射 | `ACESFilmicToneMapping` + `toneMappingExposure` | 电影级 HDR 压缩，高光不过曝 |
| 像素密度 | `setPixelRatio(Math.min(dpr, 1.5))` | 防 Retina 屏 4 倍 fill-rate 开销 |
| 帧率无关动画 | `delta = clamp(now - last, 0.001, 0.05)` | 动画不受刷新率影响，防跳帧 |

## 二、几何体（从 2D 到 3D 到顶点级）

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| `THREE.Shape` | `moveTo/lineTo/bezierCurveTo` | 用 2D 路径手绘品牌图形（三个 Trionn 臂） |
| `ExtrudeGeometry` | `{ depth: 0.42, bevelEnabled... }` | 2D 路径挤出成 3D 实体 + 倒角 |
| `EdgesGeometry` | `new EdgesGeometry(geo, 8)` | 提取锐边画线框（8° 阈值） |
| 顶点级操作 | 遍历 `position/normal` 数组 | 按法线聚合三角形 → 每个面拆成独立 Mesh（爆炸的准备） |
| 动态几何 | `setDrawRange` + `needsUpdate` | 闪电线段每帧只画前 N 个顶点 |
| 伪随机 | `Math.imul(1664525, state)` | LCG 随机数，可复现、可控 |

## 三、材质（MeshPhysicalMaterial 全家桶）

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| 金属/粗糙 | `metalness: 1, roughness: 0.08` | PBR 核心两参数 |
| **透射** | `transmission: 0.35, ior: 2.4` | 材质像玻璃一样透光（高级！） |
| **清漆层** | `clearcoat: 1, clearcoatRoughness: 0.05` | 表面额外一层高光（车漆质感） |
| 自发光 | `emissive + emissiveIntensity` | hover 闪光时动态调 |
| 环境反射 | `envMap + envMapIntensity: 3` | 靠环境贴图产生金属反射 |
| 动态改材质 | 每帧改 `roughness/transmission/envMapIntensity` | hover 闪光 = 材质参数动画 |

## 四、环境反射（商业级技巧）

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| CubeCamera | `WebGLCubeRenderTarget + CubeCamera` | 从物体中心拍 6 个面当环境贴图 |
| 性能技巧 | `environmentFrame++ % 6 === 0` 才更新 | 环境每 6 帧刷新一次，爆炸时才高频更新 |

## 五、灯光布光（摄影棚思维）

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| 三类灯光 | Ambient / Directional(9盏) / Point(3盏) | 共 13 盏灯的商业级布光 rig |
| 布光思路 | Key 主光 + Cool 补光 + Edge 轮廓光 + Warm 暖光 | 让金属在不同角度都有高光 |
| 色温对比 | 冷色主光 `0xffffff` vs 暖色 `0xff3300` | 冷暖对比是"高级感"的来源 |
| 移动灯光 | fill/warmFill 绕圈 | 移动的高光让金属"活"起来 |

## 六、粒子系统

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| `THREE.Points` | `PointsMaterial({ size: 0.022, opacity })` | 低成本粒子（600 个反射点） |
| **爆炸粒子** | 按法线分组三角形 → 每组一个 Mesh | 把模型"炸开"成几百片飞散的碎面 |
| 爆炸方向 | `radial.normalize()*0.6 + normal*0.4` | 径向 + 法线混合 = 真实炸裂感 |

## 七、交互（Raycaster）

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| 射线拾取 | `raycaster.setFromCamera(mouse, camera)` + `intersectObjects` | 鼠标点到哪个碎片 |
| hover 反馈 | `_flash` 指数衰减 + 材质参数联动 | 悬停高亮像"通电" |
| 磁力缓动 | `hoverEase = 1 - exp(-delta*k)` | 指数平滑，不突兀 |

## 八、动画状态机（核心设计）

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| 多输入仲裁 | `explodeAmt = max(scroll, hover, burst, intro)` | 多个来源统一成一个 0~1 状态量 |
| 按压状态机 | 按下 → 0.5s 蓄力(charge) → 爆炸 → 松开回落 | 时间驱动的交互设计 |
| 阻尼插值 | `x += (target - x) * k` | 所有动画都用它，丝滑的来源 |
| 相位差漂移 | 3 个 shape 用不同 `sin(elapsed + phase)` | 让部件各自独立漂移 |

## 九、Canvas 2D 叠加层（引导线）

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| 3D→2D 投影 | `Vector3.project(camera)` | 把 3D 锚点算成屏幕坐标 |
| 贝塞尔曲线 | 二次贝塞尔公式 + 切向扰动 | 画波浪引导线 |
| 鼠标最近点 | 点到线段距离检测 | 检测鼠标是否"贴着线" |
| 混合架构 | WebGL 背景 + Canvas2D 线 + DOM 前景 | 三层各干各的，互不阻塞 |

## 十、闪电特效（最炫的部分）

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| 动态 Line | `setDrawRange(0,10)` + `needsUpdate` | 一条线由 10 个顶点组成 |
| 加法混合 | `AdditiveBlending` | 发光叠加效果 |
| 6 层光晕 | 6 个圆柱体半径递减透明度递增 | 从外圈柔光到核心亮线 |
| 圆柱对齐 | `quaternion.setFromUnitVectors(up, dir)` | 圆柱沿线段方向摆放 |
| 渲染顺序 | `renderOrder` + `depthTest: false` | 特效永远画在最上层 |

## 十一、性能与工程化（专业级）

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| 延迟初始化 | `requestIdleCallback(..., {timeout:700})` + 降级 setTimeout | 首屏不被 WebGL 阻塞 |
| 响应式分级 | 4 档布局：`fov/z/scale` 按宽度切换 | 每档单独调参 |
| 资源释放 | `scene.traverse` + `dispose()` 全家桶 | 防内存泄漏 |
| 事件清理 | visibilitychange / blur / pointercancel | 切后台不卡、不留残影 |
| 无障碍 | `prefers-reduced-motion` 检测 | 尊重用户减少动画设置 |
| React 集成 | hook 生命周期 + `trionn-sound-change` 自定义事件 + ref 共享状态 | React 与命令式 Three.js 的桥接 |

## 十二、Web Audio API（你没意识到的第二技能）

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| 手势解锁 | 首次 `pointerdown/keydown` 时 `audio.resume()` | 浏览器自动播放策略 |
| 音效预加载 | `fetch → decodeAudioData` 缓存 | 触发时零延迟 |
| 合成音效 | `createOscillator` + 频率 sweep | 不用音频文件也能"哔" |
| 白噪声震动 | `ScriptProcessorNode` 随机数 + 80Hz 低通 | 蓄力时的"嗡嗡"感 |
| 音效管理 | 5 套音源互斥、淡出释放 | 不重叠、不爆音 |

## 十三、前端整合

| 知识点 | 你写的代码 | 说明 |
|--------|-----------|------|
| Astro + React | `.astro` 页面 + React 组件 | 静态页面嵌交互组件 |
| `mix-blend-mode: difference` | DOM 前景叠 WebGL 背景 | 文字与 3D 的"融合"效果 |

---

## 📊 你现在的水平定位

```
入门 ████████░░░░░░░░░░░░  20%  （场景/几何/材质/灯光基础）
进阶 ████████████████░░░░  80%  （物理材质/环境反射/布光/粒子/射线）
高级 ██████████████████░  95%  （爆炸粒子/闪电特效/音频/性能工程）
理论 ████░░░░░░░░░░░░░░░░  25%  （为什么这么调？数学原理？）
```

**强项**：动手能力、工程整合、商业级技巧（都是仿写逼出来的）
**弱项**：原理理解（不知道"为什么"）、系统化（知识是散的）

## 🎯 下一步怎么把"散装"变"系统"

1. **补完项目 TODO**（`TRIONN_IMPLEMENTATION_PROGRESS.md` 里的 7 个缺口）——最高效的巩固
2. **补"为什么"**：每块知识点配 2-3 行原理（比如：为什么 ACES？为什么 transmission 需要 CubeCamera？为什么加法混合会发光？）
3. **迁移验证**：挑第二个作品仿写，把这里学的布光/材质/音频用到新场景——能用出去才算真会
4. **你选**：我可以按上面 13 个领域，逐个给你讲原理 + 出小练习（教程形式）
