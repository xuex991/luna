import type { ThreeControls } from '../../hooks/useTrionnSymbolScene'

/** Shared production settings for every Trionn symbol scene. */
export const TRIONN_SCENE_CONTROLS: ThreeControls = {
  cameraFov: 35,
  cameraZ: 7.6,
  metalness: 1,
  roughness: 0.08,
  transmission: 0.35,
  clearcoat: 1,
  lightIntensity: 1.6,
  keyLightAngle: 45,
  rotationSpeed: 0.0042,
}
